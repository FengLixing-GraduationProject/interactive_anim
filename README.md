# 3D高斯泼溅分块训练策略与交互动画实现的可行性研究

本仓库是交互动画Demo程序

## 软件构建
1. 先将场景文件(.ply)文件放入到assets文件夹中，并重命名为 `scene.ply`
2. 打开终端，输入以下命令进行构建，构建完成后，在 `dist` 目录可以找到构建好的软件
```cmd
npm run build
```

## 软件开发 & 调试
```cmd
npm run dev
```
